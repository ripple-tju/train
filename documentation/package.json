{
	"name": "@ripple.tju/train-documentation",
	"version": "0.0.0",
	"description": "docs data and validator",
	"main": "index.js",
	"scripts": {
		"check": "node ./script/check.mjs",
		"build": "node ./script/build.mjs",
		"deploy": "npm run build && node ./script/deploy.mjs",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "UNLICENSED",
	"dependencies": {
		"ajv": "^8.17.1",
		"ajv-formats": "^3.0.1"
	},
	"devDependencies": {
		"@produck/idiom": "^0.4.0",
		"@produck/ow": "^0.1.2"
	}
}
